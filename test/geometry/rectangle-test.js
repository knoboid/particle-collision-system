import { expect } from 'chai';

import Rectangle from '../../src/geometry/rectangle';

describe('Tests for state changes', () => {

    it('test reset', () => {
        expect(0).to.equal(0);
    });

});